apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synology-nfs-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: synology-nfs
  resources:
    requests:
      storage: 10Gi
