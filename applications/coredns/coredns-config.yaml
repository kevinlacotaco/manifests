apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  Corefile.override: |
    .:53 {
      hosts custom.hosts {
        192.168.68.138 hoist.local
        fallthrough
      }
      forward . /etc/resolv.conf
      log
    }
