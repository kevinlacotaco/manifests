apiVersion: v1
kind: Service
metadata:
  name: adguard-home
  namespace: networking
spec:
  type: LoadBalancer
  selector:
    app: adguard-home
  ports:

    - name: web
      port: 3001
      targetPort: 3000
      nodePort: 31628